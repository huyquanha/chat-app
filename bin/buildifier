#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

# TODO: Add support for -mode check and -lint warn
exec bazel run @buildifier_prebuilt//:buildifier -- -r "${REPO_ROOT}"